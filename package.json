{
	"name": "paseo-core",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "node scripts/deploy-and-show.js",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types",
		"demo": "source .env && node examples/demo.mjs"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.0.7",
		"wrangler": "^4.19.1"
	},
	"dependencies": {
		"zod": "^4.0.15"
	}
}
